-r requirements-core.txt
-r requirements-dashboard.txt

# IMPORTANT: torch/torchvision are intentionally NOT installed here.
# On RunPod, use the CUDA-enabled PyTorch already included in the pod image.
# scripts/setup_runpod.sh verifies it and refuses to silently replace it with
# a potentially CPU-only or CUDA-mismatched wheel.
